"""
Backend test package — Pytest configuration.

These tests run against an in-memory SQLite-compatible schema; they do not
require a live PostgreSQL/Redis. The full integration suite lives in
backend/tests/integration/.
"""
